{
  "name": "bougie",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Arene PvPvE navigateur — prototype",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @bougie/client",
    "build": "npm run build --workspace @bougie/client",
    "preview": "npm run preview --workspace @bougie/client",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
